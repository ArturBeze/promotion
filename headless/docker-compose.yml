#version: "3.9"

services:
  playwright_bot:
    build: .
    image: art-playwright-bot:latest
    deploy:
      replicas: 1
      restart_policy:
        condition: any
    volumes:
      - ./screenshots:/app/screenshots
    environment:
      - TZ=UTC

volumes:
  screenshots: