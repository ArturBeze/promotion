#version: "3.9"

services:
  playwright_bot:
    build: .
    image: art-playwright-bot:latest
#    container_name: bot_1
    deploy:
      replicas: 6
      restart_policy:
        condition: any
    volumes:
      - ./screenshots:/app/screenshots
    environment:
      - TZ=UTC

volumes:
  screenshots: